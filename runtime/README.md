# Hafez Studio Offline Runtime

This folder is part of the portable product and must stay beside the desktop
application resources. It contains the local Persian speech model and the
FFmpeg tools used by the edit engine. It contains no user footage, Telegram
credentials, API keys or n8n configuration.

The path can be overridden with `HERMES_RUNTIME_ROOT` for managed deployments.
