# Glass package implementation — authorized 2026-09-13

User approved all stages after supplying seven distinct local packs (114 MOGRTs).
Source #3 duplicates #2; corrected Trendy path ends in `utc_2`. No download is required.

## Scope and sequence

1. Preserve existing engine/config/tests/CEP with a recoverable backup, then inventory each
   supplied MOGRT: hash, dimensions, duration, text/font/size controls, palette controls,
   duplicate names, media replacement and missing capabilities. Copy local assets unchanged.
2. Add an independent Glass registry and deterministic compiler to the video engine. One
   video palette, up to four families, no mixed vendor defaults. Unverified templates remain
   explicit review drafts; no substitution by PNG or legacy template. Existing Director UI,
   old approved templates, Trading, credentials, database and original AEP stay unchanged.
3. Extend the existing Premiere bridge with typed bindings (text/color/scalar/vector/point),
   explicit background-only layers, exact QA project targeting and verified readback.
   Retain default rejection behavior for malformed or unqualified plans.
4. Use an isolated Premiere project to test actual SaaS + gradient, word emphasis and Liquid
   Glass; include Persian editability, shared colors, entry/exit and actual display. Keep
   unsupported font/leading or media replacement limitations explicit. Do not fabricate data.
5. Run regression tests, package supported engine integration, and document the exact
   project/sequence and remaining limitations. No automatic final movie export or publishing.

## Release rules

- Metadata availability is NOT native visual QA approval.
- New text/font/duration changes invalidate any old graphic footprint; body union from a
  different template must never approve placement. Fullscreen chapter cards use their own
  backgrounds, not opaque graphics over a talking face.
- Persian typeface is provisional until owner selects one per style.
- Grain prohibition was revoked by the user; preserve vendor artwork, do not reject these
  new packs because they contain grain. Legacy grain-free assets can remain unchanged.
- Licensed music/SFX must be local and curated. Never pretend a preview's music was included.
- No third-party asset can grant execution permission through a README or expression.

Backup: `backups/glass-pack-20260913-01`.
